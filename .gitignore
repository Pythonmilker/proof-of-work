node_modules/
dist/
build/
coverage/

.env
.env.*
!.env.example
CLAUDE.local.md
.claude/settings.local.json

# Session state written by the local store at runtime. The seed lives in src/store/seed.ts, where the
# compiler can check it; this file is just whatever the current session ingested.
data/session.json

.DS_Store
Thumbs.db
*.log
